# Test Runtimes Skill

This skill declares runtime dependencies for testing Capacium pre-flight checks.
It requires uv and node to be installed on the host.

## Usage

This skill exists solely for automated testing of `cap install` runtime validation.
